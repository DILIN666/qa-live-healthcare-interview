# API 测试文档

## 概述

本文档介绍如何使用项目中提供的API测试脚本来测试医生列表页面的后端API接口。

## 测试脚本说明

项目提供了两个版本的测试脚本：

1. **test-doctor-api.ps1** - PowerShell版本（适用于Windows环境）
2. **test-doctor-api.sh** - Bash版本（适用于Linux/Mac环境）

## 前置条件

在运行测试脚本之前，请确保：

1. Docker容器已启动（MySQL数据库）
2. 后端服务 `qa-service-user` 已启动并监听在 `http://localhost:8080`
3. 数据库已初始化并包含测试数据

### 启动Docker容器

```bash
docker-compose up -d
```

### 启动后端服务

```bash
cd server/qa-service-user
./mvnw spring-boot:run
```

## 测试场景

测试脚本包含以下测试场景：

### 1. 正常请求测试
- 获取医生列表数据的正常请求
- 验证返回格式和数据完整性

### 2. 分页参数测试
- 第1页，每页10条
- 第2页，每页5条
- 第3页，每页20条

### 3. 排序功能测试
- 按ID升序排序
- 按ID降序排序

### 4. 筛选条件测试
- 按科室筛选（心内科）
- 按活跃状态筛选（在线）
- 组合筛选（心内科 + 在线）

### 5. 错误处理测试
- 无效的页码
- 无效的每页条数
- 无效的排序字段

### 6. 单个医生详情测试
- 存在的医生详情查询
- 不存在的医生详情查询（404错误）

### 7. 响应时间验证
- 所有接口的响应时间应不超过500ms

## 运行测试

### Windows环境

使用PowerShell运行测试脚本：

```powershell
# 方式1：直接运行
.\test-doctor-api.ps1

# 方式2：如果遇到执行策略限制
powershell -ExecutionPolicy Bypass -File test-doctor-api.ps1
```

### Linux/Mac环境

使用Bash运行测试脚本：

```bash
# 添加执行权限
chmod +x test-doctor-api.sh

# 运行测试
./test-doctor-api.sh
```

## 测试结果解读

测试脚本执行完成后会输出：

1. 每个测试用例的执行状态（通过/失败）
2. 失败用例的详细错误信息
3. 每个请求的响应时间
4. 最终的测试统计（通过数、失败数）

### 输出示例

```
====================================
开始测试医生列表API接口
====================================
测试: 正常请求 - 获取医生列表... 通过 (响应时间: 123.45ms)
测试: 分页测试 - 第1页，每页10条... 通过 (响应时间: 45.67ms)
...
====================================
测试完成
通过: 15
失败: 0
====================================
所有测试用例通过！
```

## 自定义测试

### 修改API基础URL

如果后端服务不在 `http://localhost:8080`，可以修改脚本中的 `$API_URL` 或 `API_URL` 变量。

### 添加新的测试用例

可以参考现有测试用例的格式，在脚本中添加新的测试场景：

**PowerShell版本:**
```powershell
Run-Test "测试名称" "API_URL" 预期状态码 "预期内容"
```

**Bash版本:**
```bash
run_test "测试名称" "curl命令" "预期状态码" "预期内容"
```

## 常见问题

### Q: 提示"无法连接到服务器"
A: 请确保后端服务已启动，并且API URL配置正确。

### Q: 所有测试都失败
A: 检查数据库是否已初始化，确认 `mysql-init/init.sql` 中的数据已正确导入。

### Q: 响应时间超过500ms
A: 这只是警告，不影响测试通过。可以优化数据库查询或服务器性能。

### Q: PowerShell脚本无法执行
A: 尝试使用 `-ExecutionPolicy Bypass` 参数临时绕过执行策略限制。
